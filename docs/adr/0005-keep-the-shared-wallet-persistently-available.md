# Keep the shared wallet persistently available

The extension will keep the operator-imported Shared Deployment Wallet available across browser restarts without requiring a recurring unlock step. This prioritizes immediate deployment for trusted internal Operators and explicitly accepts that access to an Operator's browser profile or extension storage may expose the wallet key; the key remains excluded from source code and the distributed extension package.

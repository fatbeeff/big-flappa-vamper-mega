# Bundle the payment-asset manifest

The extension renders Crypto/RWA payment assets from one validated manifest bundled into each release. A remote registry had no authoritative upstream or deployed endpoint, so its server, cache, alarm, and refresh UI added failure modes without improving freshness. Asset changes ship through the reviewed release process.

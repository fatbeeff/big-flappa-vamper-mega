# Start from GMGN token surfaces

The extension injects its actions into supported GMGN Trenches cards and token pages. This keeps discovery, inspection, and correction in the surface Operators already use while accepting a dependency on GMGN's page structure.

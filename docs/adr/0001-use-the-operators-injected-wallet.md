# Use the Operator's injected wallet

The extension delegates account access and transaction signing to the EIP-1193 wallet selected by the browser, such as MetaMask or Rabby. It stores no private keys. Wallet prompts add a confirmation step and work across Flap and PONS; Long manages its own launch signatures.
